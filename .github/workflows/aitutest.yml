name: Cypress Tests
on: [push]
jobs:
  cypress-run:   
     runs-on: windows-latest
     steps:
        - name: Cypress run
          uses: cypress-io/github-action@v5
          with: 
            build: npm run build
            start: npm start
# 